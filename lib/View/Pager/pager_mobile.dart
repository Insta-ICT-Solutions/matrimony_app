import 'package:flutter/material.dart';

class PagerMobile extends StatelessWidget {
  const PagerMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
